const userController = require("./user.controller");
const postController = require("./post.controller");
const tagController = require("./tag.controller");

module.exports = { userController, postController, tagController };
